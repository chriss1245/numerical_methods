import numpy as np
f = lambda x: x**2
g = lambda θ: np.sin(θ)
h = lambda x: 2
i = lambda x: 2*x

a = 0 # lower bound
b = 1 # upper bound
c = np.pi # uperbound for sinosoidal functions
m = 20 # number of partitions

f1 = 1/3 #result of evaluatind f
g1 = -1
h1 = 2
i1 = 1
